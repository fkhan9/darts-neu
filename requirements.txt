# Install torch/torchvision separately first with the CUDA-specific command below,
# then run `pip install -r requirements.txt` for the rest.
# GPU (NVIDIA RTX 3060, CUDA 13.0 driver):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
numpy
pyyaml
tqdm
matplotlib